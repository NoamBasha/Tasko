import React from "react";

const BoardTab = ({ name, id, edit, setName }) => {
    //TODO change redux state to board "id"
    //TODO use edit to change title - like in ColumnContainer in the title change
    return (
        <div>
            <a href="">{name}</a>
        </div>
    );
};

export default BoardTab;
